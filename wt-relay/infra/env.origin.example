# origin(隠す側 ARM, karutte) の設定。コピーして env.origin に(gitignore 済み)。
# 実 IP・鍵・エンドポイントの具体値は env.origin にだけ書く。この example と docs には書かない。

# relay で setup-relay.sh を一度走らせると表示される公開鍵
RELAY_WG_PUBKEY=

# relay の公開 IP:WGポート。例: <relay-public-ip>:51820
# origin が外向きに繋ぐ(origin 側はポートを一つも開けない)。
RELAY_ENDPOINT=
