# relay(公開側 x64) の設定。コピーして env.relay に(gitignore 済み)。
# 実 IP・鍵・エンドポイントの具体値は env.relay にだけ書く。この example と docs には書かない。

# origin で setup-origin.sh を一度走らせると表示される公開鍵
ORIGIN_WG_PUBKEY=

# WG の待ち受けポート(既定 51820)。OCI セキュリティリストでも UDP で開けること。
#WG_PORT=51820
